{
  "name": "api",
  "version": "1.0.0",
  "main": "index.ts",
  "dependencies": {
    "drizzle-orm": "0.45.1",
    "exceljs": "4.4.0",
    "hono": "4.11.1",
    "uuid": "13.0.0",
    "xlsx": "0.18.5",
    "zod": "4.2.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20251217.0",
    "@types/node": "25.0.3",
    "drizzle-kit": "0.31.8",
    "ts-standard": "12.0.2",
    "wrangler": "4.55.0"
  },
  "eslintConfig": {
    "parserOptions": {
      "project": "./tsconfig.json"
    },
    "extends": [
      "./node_modules/ts-standard/eslintrc.json"
    ]
  },
  "scripts": {
    "lint": "ts-standard",
    "lint:fix": "ts-standard --fix",
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "db:schema": "wrangler d1 execute drinks-api-db --remote --file=schema.sql",
    "deploy:init": "pnpm run deploy && pnpm run db:schema"
  }
}
